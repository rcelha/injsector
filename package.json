{
  "name": "injsector",
  "version": "1.0.0",
  "scripts": {
    "test": "standard --verbose | snazzy",
    "build": "mkdir -p ./dist && browserify -t [ babelify ] main.js -o dist/bundle.js"
  },
  "dependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.3.0",
    "react": "^15.1.0",
    "react-dom": "^15.1.0"
  },
  "devDependencies": {
    "browserify": "^13.0.1",
    "pre-commit": "^1.1.3",
    "snazzy": "^4.0.0",
    "standard": "^7.1.2"
  }
}
